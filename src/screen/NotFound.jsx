import React from 'react';

export default function NotFound() {
  return (
    <div
      style={{
        color: '#fff',
        height: '50vh',
        color: '#000',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      NotFound
    </div>
  );
}
